@model Biashara_POS.DTOs.CreateStockMeasureDto

@{
    ViewData["Title"] = "Create Stock Measure";
}

<div class="container mt-4">
    <h2>Create Stock Measure</h2>

    <form asp-action="Create" method="post">

        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <label asp-for="MeasureName" class="form-label"></label>
            <input asp-for="MeasureName" class="form-control" />
            <span asp-validation-for="MeasureName" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Initials" class="form-label"></label>
            <input asp-for="Initials" class="form-control" />
            <span asp-validation-for="Initials" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-success">Save</button>
        <a asp-action="Index" class="btn btn-secondary">Cancel</a>

    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}